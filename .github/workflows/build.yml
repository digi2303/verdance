# Automatically build the project and run any configured tests for every push
# and submitted pull request. This can help catch issues that only occur on
# certain platforms or Java versions, and provides a first line of defence
# against bad commits.

name: build
on: [pull_request, push]

jobs:
  build:
    strategy:
      matrix:
        # Use these Java versions
        java: [
          25,    # Minimum supported by Minecraft 26.1
        ]
        # and run on both Linux and Windows
        os: [ubuntu-22.04, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4
      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v4
      - name: setup jdk ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
      - name: make gradle wrapper executable
        if: ${{ runner.os != 'Windows' }}
        run: chmod +x ./gradlew
      - name: build
        # Stonecutter swaps source in-place per active node, so each version must be
        # switched and built in its own Gradle invocation (one-shot chiseledBuild can't
        # swap-then-compile reliably). Add new nodes to this list.
        shell: bash
        run: |
          for node in yumi-26.1 yumi-26.2; do
            ./gradlew "Set active project to $node" --quiet
            ./gradlew ":$node:build"
          done
          ./gradlew "Set active project to yumi-26.1" --quiet
      - name: capture build artifacts
        if: ${{ runner.os == 'Linux' && matrix.java == '25' }} # Only upload artifacts built from one OS
        uses: actions/upload-artifact@v4
        with:
          name: Artifacts
          path: versions/*/build/libs/
